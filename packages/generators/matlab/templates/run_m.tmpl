%% {{.ProjectName}}
%  Generated by AIStudio MATLAB Generator
%  Workflow: {{.WorkflowName}} ({{.WorkflowID}})

function run()
    % RUN Main execution script for the AIStudio workflow.
    %   Executes each node in topological order, passing data
    %   between nodes through a shared state structure.
    %
    %   Usage: run()
    
    fprintf('Starting workflow: %s\n', '{{.WorkflowName}}');
    
    % Ensure project is initialized
    if ~isappdata(0, 'ProjectRoot')
        startup();
    end
    
    % Initialize shared state
    state = struct();
    
    % Execute workflow nodes in DAG order
    {{range .Executions}}
    % Node: {{.NodeName}} ({{.NodeType}})
    fprintf('Executing node: {{.NodeName}}...\n');
    state = {{.ScriptName}}(state);
    fprintf('Node {{.NodeName}} complete.\n');
    {{end}}
    
    fprintf('Workflow completed successfully.\n');
end

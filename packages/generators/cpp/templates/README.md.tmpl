# {{.ProjectName}}

{{.Description}}

## Overview

This C++ project was generated by AIStudio from workflow: **{{.WorkflowName}}**.

## Project Structure

```
.
├── include/             # Header files (.hpp)
│   ├── data_loader.hpp
│   └── ...
├── src/                 # Source files (.cpp)
│   ├── CMakeLists.txt   # Source module build
│   ├── main.cpp         # Entry point
│   └── ...
├── tests/               # Unit tests
│   ├── CMakeLists.txt   # Test module build
│   ├── test_main.cpp
│   └── test_nodes.cpp
├── CMakeLists.txt       # Top-level build
├── vcpkg.json           # Vcpkg dependencies
├── conanfile.txt        # Conan dependencies (alternative)
├── workflow.json        # Original workflow definition
├── README.md            # This file
└── .gitignore
```

## Prerequisites

- CMake 3.20+
- C++17 compatible compiler (g++ 10+, clang 12+)
- Make or Ninja
- vcpkg or conan (optional, for dependencies)

## Quick Start

### Configure with CMake
```bash
cmake -B build -S .
```

### Build
```bash
cmake --build build
```

### Run
```bash
./build/{{.ProjectName}}
```

### Run tests
```bash
cmake --build build --target test
```

### Using vcpkg
```bash
cmake -B build -S . -DCMAKE_TOOLCHAIN_FILE=/path/to/vcpkg/scripts/buildsystems/vcpkg.cmake
```

## Workflow Nodes

| Node | Class | Description |
|------|-------|-------------|
{{range .Nodes}}| {{.Name}} | `{{.ClassName}}` | {{.Description}} |
{{end}}

---
*Generated by AIStudio*

// {{.ProjectName}}
// Generated by AIStudio C++ Generator
// Workflow: {{.WorkflowName}} ({{.WorkflowID}})

#include <iostream>
#include <memory>
{{range .Executions}}#include "{{.HeaderName}}.hpp"
{{end}}

int main(int argc, char* argv[])
{
    std::cout << "Starting workflow: {{.WorkflowName}}" << std::endl;

    {{range .Executions}}
    // Node: {{.NodeName}} ({{.NodeType}})
    {
        {{.ClassName}} node;
        node.execute();
    }
    {{end}}

    std::cout << "Workflow completed successfully." << std::endl;
    return 0;
}

# Source module for {{.ProjectName}}

set(SOURCES
    main.cpp
{{range .Executions}}    {{.HeaderName}}.cpp
{{end}})

add_executable(${PROJECT_NAME} ${SOURCES})

target_include_directories(${PROJECT_NAME} PRIVATE
    ${CMAKE_CURRENT_SOURCE_DIR}/../include
)

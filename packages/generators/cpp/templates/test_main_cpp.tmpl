// Tests for {{.ProjectName}}
// Generated by AIStudio C++ Generator

#include <catch2/catch_test_macros.hpp>
{{range .Executions}}#include "{{.HeaderName}}.hpp"
{{end}}

TEST_CASE("Workflow nodes can be instantiated", "[nodes]")
{
    {{range .Executions}}
    SECTION("{{.NodeName}}")
    {
        {{.ClassName}} node;
        REQUIRE_NOTHROW(node.execute());
    }
    {{end}}
}

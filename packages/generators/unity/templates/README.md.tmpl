# {{.ProjectName}}

{{.Description}}

## Overview

This Unity C# package was generated by AIStudio from workflow: **{{.WorkflowName}}**.

## Project Structure

```
.
├── Runtime/                     # Runtime scripts
│   ├── AistudioWorkflow.asmdef  # Runtime assembly definition
│   ├── WorkflowRunner.cs        # Main workflow runner
│   ├── DataLoaderNode.cs        # Per-node implementations
│   └── ...
├── Editor/                      # Editor scripts
│   ├── AistudioWorkflow.Editor.asmdef
│   └── WorkflowEditor.cs
├── Scripts/                     # Legacy scripts folder
├── package.json                 # UPM package manifest
├── workflow.json                # Original workflow definition
├── README.md                    # This file
└── .gitignore
```

## Prerequisites

- Unity 2021.3 LTS or newer
- (Optional) Git for UPM installation

## Quick Start

### Install as UPM package
In Unity, open Window > Package Manager > Add package from git URL.

### Or copy to Assets folder
Copy the `Runtime` and `Editor` folders into your Unity project's `Assets` folder.

### Create a WorkflowRunner
Attach the `WorkflowRunner` component to a GameObject and call `ExecuteWorkflow()`.

## Workflow Nodes

| Node | Class | Description |
|------|-------|-------------|
{{range .Nodes}}| {{.Name}} | `{{.ClassName}}` | {{.Description}} |
{{end}}

---
*Generated by AIStudio*

# Tests for {{.PackageName}} ROS2 package
# Generated by AIStudio

import unittest


class TestWorkflowNodes(unittest.TestCase):
    """Test suite for workflow nodes."""

    def test_package_imports(self):
        """Verify all node modules can be imported."""
        {{range .Executions}}
        try:
            import aistudio_ws.{{.NodeNameClean}}
            self.assertTrue(True)
        except ImportError as e:
            self.fail(f"Failed to import {{.NodeNameClean}}: {e}")
        {{end}}


if __name__ == '__main__':
    unittest.main()

# {{.ProjectName}}

{{.Description}}

## Overview

This ROS2 package was generated by AIStudio from workflow: **{{.WorkflowName}}**.

## Project Structure

```
.
├── aistudio_ws/         # Node implementations
│   ├── __init__.py
│   ├── data_loader_node.py
│   └── ...
├── launch/              # Launch files
│   └── {{.PackageName}}_launch.py
├── resource/            # ROS2 resource marker
├── test/                # Test files
│   ├── __init__.py
│   └── test_nodes.py
├── package.xml          # ROS2 package manifest
├── setup.py             # Package setup
├── setup.cfg            # Setup config
├── workflow.json        # Original workflow definition
├── README.md            # This file
└── .gitignore
```

## Prerequisites

- ROS2 Humble or Jazzy
- Python 3.8+
- colcon

## Quick Start

### Build the package
```bash
cd {{.PackageName}}
colcon build --packages-select {{.PackageName}}
```

### Source the workspace
```bash
source install/setup.bash
```

### Run the workflow launch file
```bash
ros2 launch {{.PackageName}} {{.PackageName}}_launch.py
```

### Run tests
```bash
colcon test --packages-select {{.PackageName}}
```

## Workflow Nodes

| Node | ROS2 Node | Description |
|------|-----------|-------------|
{{range .Nodes}}| {{.Name}} | {{.NodeName}} | {{.Description}} |
{{end}}

---
*Generated by AIStudio*

# {{.NodeName}} - {{.NodeID}}
# {{.NodeType}}
# Generated by AIStudio ROS2 Generator

import rclpy
from rclpy.node import Node


class {{.ClassName}}(Node):
    """ROS2 node implementation for {{.NodeName}}."""

    def __init__(self):
        super().__init__('{{.NodeName}}')
        self.get_logger().info('{{.NodeName}} node initialized')

    def execute(self):
        """Execute the node logic."""
        self.get_logger().info('Executing {{.NodeName}}')
        # TODO: Implement {{.NodeType}} node logic


def main(args=None):
    rclpy.init(args=args)
    node = {{.ClassName}}()
    try:
        node.execute()
        rclpy.spin_once(node, timeout_sec=1.0)
    except KeyboardInterrupt:
        pass
    finally:
        node.destroy_node()
        rclpy.shutdown()


if __name__ == '__main__':
    main()

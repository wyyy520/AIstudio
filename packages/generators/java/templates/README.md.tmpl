# {{.ProjectName}}

{{.Description}}

## Overview

This Java Maven project was generated by AIStudio from workflow: **{{.WorkflowName}}**.

## Project Structure

```
.
├── src/
│   ├── main/
│   │   └── java/
│   │       └── {{.PackagePath}}/
│   │           ├── App.java               # Main entry point
│   │           ├── DataLoaderNode.java    # Per-node implementations
│   │           └── ...
│   └── test/
│       └── java/
│           └── {{.PackagePath}}/
│               └── AppTest.java           # Test stubs
├── pom.xml                    # Maven build
├── workflow.json              # Original workflow definition
├── README.md                  # This file
└── .gitignore
```

## Prerequisites

- JDK 17+
- Apache Maven 3.8+

## Quick Start

### Build the project
```bash
mvn package
```

### Run the application
```bash
mvn exec:java -Dexec.mainClass="{{.PackageName}}.App"
```

### Run tests
```bash
mvn test
```

### Create runnable JAR
```bash
mvn package -DskipTests
java -jar target/{{.ProjectName}}-{{.Version}}.jar
```

## Workflow Nodes

| Node | Class | Description |
|------|-------|-------------|
{{range .Nodes}}| {{.Name}} | `{{.ClassName}}` | {{.Description}} |
{{end}}

---
*Generated by AIStudio*

# 2026-07-07 工作日志

## AIStudio 项目自动推送脚本

- 在项目根目录 `D:\Personal Project\AI Studio\AIStudio\` 创建了 `auto_push.bat` 自动推送脚本
- 远程仓库: https://github.com/wyyy520/AIstudio.git
- 脚本功能：自动初始化git（如需要）、配置远程仓库、添加所有更改、带时间戳提交、推送到master分支
- 配置了 `git config --global credential.helper store`，首次输入凭据后自动记住
- 当时项目状态：无任何提交、无远程仓库配置，分支为master
- Git全局配置已存在：用户名 wyyy520，邮箱 ntrn389989@163.com

### 编码问题修复

- bat文件首次以UTF-8编码写入，导致cmd用GBK解析时中文注释字节被截断，破坏命令行语法
- 错误现象：`'om' is not recognized`、`'OTE_URL"'` 等命令截断错误
- 解决方案：用Python以GBK编码重新写入bat文件，保留中文注释和输出
- 经验：Windows bat文件必须使用GBK/ANSI编码保存，否则中文会导致命令解析错误

### 第二次运行修复

- bat中 `chcp 65001` 导致GBK编码的中文在UTF-8代码页下显示为乱码，已去掉
- 时间戳用 `%date% %time%` 解析在中文Windows下格式异常（"周二-2026-07007:21:53"），改用 PowerShell `Get-Date -Format` 生成
- 提交成功：commit 77ee3e0，235个文件，41751行插入

### 网络与认证配置

- 检测到本地Clash Verge代理端口7897，配置 `git config --global http.proxy/https.proxy http://127.0.0.1:7897`
- 配置 `git config --global core.quotepath false`（中文文件名正常显示）
- 配置 `git config --global core.autocrlf true`（减少LF/CRLF警告）
- SSH方式尝试失败：connect.exe通过SOCKS5代理连接github.com:22和ssh.github.com:443均在 `kex_exchange_identification` 阶段被关闭，疑似Clash对SSH流量干扰
- 最终采用HTTPS+代理方式，curl验证返回200，网络通畅
- credential.helper store已配置，用户需在cmd中手动首次认证（输入用户名+Token），之后自动记住
- SSH公钥已存在：`~/.ssh/id_ed25519`（ed25519），SSH config已配置但未使用

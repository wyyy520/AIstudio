{
  "id": "cuda-env",
  "name": "CUDA 环境",
  "version": "2.0.0",
  "min_schema_version": "2.0.0",
  "kind": "system",
  "description": "CUDA environment detection, GPU monitoring, and configuration management",
  "author": "AIStudio",
  "nodes": [
    {
      "type": "system.cuda_detect",
      "name": "CUDA 检测",
      "description": "检测 CUDA 可用性和 GPU 信息",
      "inputs": [],
      "outputs": [
        { "id": "cuda_info", "name": "CUDA 信息", "type": "json" }
      ],
      "config_schema": {
        "type": "object",
        "properties": {
          "check_torch": { "type": "boolean", "default": true, "description": "是否检查 PyTorch CUDA 可用性" },
          "min_version": { "type": "string", "default": "11.0", "description": "最低 CUDA 版本要求" }
        }
      }
    }
  ],
  "runtime_bundle": "cuda-tools",
  "supported_targets": ["python"]
}
